[package]
name = "ai_blaise_citus_pool_wire"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

# FEATURE: T7
# PostgreSQL v3 wire-protocol codec for the pool's extended-query buffer +
# cancel-request routing + startup tap. Modeled after the Rust port plan
# derived from jackc/pgx pgproto3 (MIT). No external dependencies on purpose:
# the codec works on &[u8] / Vec<u8> so it composes cleanly with the std-only
# byte-transparent proxy code path in pool/src/proxy.rs.
[dependencies]
