[package]
name = "ai_blaise_citus_companion"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["rlib"]

[features]
default = []
pg18 = ["dep:pgrx", "pgrx/pg18"]

[dependencies]
ai_blaise_citus_sidecar_shared = { path = "../sidecar/shared" }
pgrx = { version = "0.18", default-features = false, optional = true }
