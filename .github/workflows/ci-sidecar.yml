name: sidecar

on:
  pull_request:
  push:
    branches:
      - main
      - ai-blaise/dev

jobs:
  shared:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Format
        run: cargo fmt --all -- --check
      - name: Check tests
        run: cargo check -p ai_blaise_citus_sidecar_shared --tests
      - name: Test
        run: cargo test -p ai_blaise_citus_sidecar_shared

  vectorizer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Format
        run: cargo fmt --all -- --check
      - name: Check tests
        run: cargo check -p ai_blaise_citus_sidecar_vectorizer --tests
      - name: Test
        run: cargo test -p ai_blaise_citus_sidecar_vectorizer

  auth:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Format
        run: cargo fmt --all -- --check
      - name: Check tests
        run: cargo check -p ai_blaise_citus_sidecar_auth --tests
      - name: Test
        run: cargo test -p ai_blaise_citus_sidecar_auth

  backup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Format
        run: cargo fmt --all -- --check
      - name: Check tests
        run: cargo check -p ai_blaise_citus_sidecar_backup --tests
      - name: Test
        run: cargo test -p ai_blaise_citus_sidecar_backup

  cdc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Format
        run: cargo fmt --all -- --check
      - name: Check tests
        run: cargo check -p ai_blaise_citus_sidecar_cdc --tests
      - name: Test
        run: cargo test -p ai_blaise_citus_sidecar_cdc

  storage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Format
        run: cargo fmt --all -- --check
      - name: Check tests
        run: cargo check -p ai_blaise_citus_sidecar_storage --tests
      - name: Test
        run: cargo test -p ai_blaise_citus_sidecar_storage
